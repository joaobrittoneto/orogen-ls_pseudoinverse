# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(ls_pseudoinverseTaskLib)
ADD_LIBRARY(${LS_PSEUDOINVERSE_TASKLIB_NAME} SHARED 
    ${LS_PSEUDOINVERSE_TASKLIB_SOURCES})
add_dependencies(${LS_PSEUDOINVERSE_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${LS_PSEUDOINVERSE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${LS_PSEUDOINVERSE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${LS_PSEUDOINVERSE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${LS_PSEUDOINVERSE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${LS_PSEUDOINVERSE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${LS_PSEUDOINVERSE_TASKLIB_HEADERS}
    DESTINATION include/orocos/ls_pseudoinverse)

